# Vault MCP — no auth required.
# All operations are local-only filesystem reads against the repo's
# vault/wiki/ tree and vault/.index/manifest.json.
#
# Server resolves paths via CLAUDE_PROJECT_DIR (set by Claude Code on spawn)
# with a fallback to walking up from mcps/vault/.
